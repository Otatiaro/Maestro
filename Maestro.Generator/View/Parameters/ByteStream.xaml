<UserControl x:Class="Maestro.Generator.View.Parameters.ByteStream"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="Byte Stream">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <CheckBox Content="Can _Read" IsChecked="{Binding CanRead}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2"/>
            <CheckBox Content="Can _Write" IsChecked="{Binding CanWrite}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="2"/>
        </Grid>
    </GroupBox>
</UserControl>
